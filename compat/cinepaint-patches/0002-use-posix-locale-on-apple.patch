--- a/hollywood/app/app_procs.c
+++ b/hollywood/app/app_procs.c
@@ -504,7 +504,7 @@ fl_initialise_locale(const char* domain, const char* locale_path,
     const char* tmp = 0;
     const char* loc = NULL;
     const char* bdtd = 0;
-# ifdef __APPLE__
+# if 0
     // 1. get the locale info
     CFLocaleRef userLocaleRef = CFLocaleCopyCurrent();
     CFStringRef cfstring = CFLocaleGetIdentifier(userLocaleRef);
